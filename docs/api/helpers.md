# Helpers

```python
from paperthin.helpers import DownloadButton
```

Low-level building blocks for custom entries. `DownloadButton` is currently the
only one you need: an adapter returns these from `get_buttons` to attach
downloadable data to an entry.

```{eval-rst}
.. autoclass:: paperthin.helpers.DownloadButton
   :members:
```
